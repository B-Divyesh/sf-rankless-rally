[
  {
    "id": "complete-run",
    "claim": "Finish a routed board and see a three-part rally card.",
    "where": "game end screen, README",
    "test": "npm test -- --grep @claim:complete-run",
    "sandbox": "fresh browser context at /demo; keyboard route reaches the exit"
  },
  {
    "id": "ninety-second-run",
    "claim": "Start a standard board with 90 seconds.",
    "where": "game first screen, README",
    "test": "npm test -- --grep @claim:ninety-second-run",
    "sandbox": "fresh browser context at /demo; inspect the standard timer"
  },
  {
    "id": "free-no-account",
    "claim": "Start sample play without a sign-in or payment step.",
    "where": "game first screen, terms, README",
    "test": "npm test -- --grep @claim:free-no-account",
    "sandbox": "fresh browser context at /demo; start the board directly"
  },
  {
    "id": "restart-reset",
    "claim": "Restart clears the timer, route, and run score.",
    "where": "game controls, README",
    "test": "npm test -- --grep @claim:restart-reset",
    "sandbox": "fresh browser context at /demo; finish then restart"
  },
  {
    "id": "settings-persist",
    "claim": "Save assist and reduced-movement settings in this browser.",
    "where": "settings dialog, README",
    "test": "npm test -- --grep @claim:settings-persist",
    "sandbox": "fresh browser context at /demo; change settings and reload"
  },
  {
    "id": "assist-extra-time",
    "claim": "Give a new assisted run 135 seconds.",
    "where": "settings dialog, README",
    "test": "npm test -- --grep @claim:assist-extra-time",
    "sandbox": "fresh browser context at /demo; enable Assist mode and inspect the timer"
  },
  {
    "id": "demo-isolation",
    "claim": "Reset demo data without changing a real game run.",
    "where": "demo banner, README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "one context holds a real run, enters /demo, resets, then returns"
  },
  {
    "id": "keyboard-controls",
    "claim": "Route with Arrow keys and WASD.",
    "where": "game controls, README",
    "test": "npm test -- --grep @claim:keyboard-controls",
    "sandbox": "fresh browser context at /demo; use ArrowRight then D to connect Relay 1"
  },
  {
    "id": "touch-controls",
    "claim": "Route with 44-pixel on-screen direction controls.",
    "where": "game controls, README",
    "test": "npm test -- --grep @claim:touch-controls",
    "sandbox": "fresh browser context at /demo; use a real phone tap or desktop pointer click to connect Relay 1"
  },
  {
    "id": "pause-recovery",
    "claim": "Restore an unfinished run after a refresh.",
    "where": "README",
    "test": "npm test -- --grep @claim:pause-recovery",
    "sandbox": "fresh browser context at /demo; pause after a move and reload"
  },
  {
    "id": "shared-replay",
    "claim": "Open a server-checked replay code in another browser without an account.",
    "where": "end screen, replay section, README",
    "test": "npm test -- --grep @claim:shared-replay",
    "sandbox": "finish /demo board, copy displayed code, load it in a second browser context"
  },
  {
    "id": "server-verified-replay",
    "claim": "Get a share code only after this product's server verifies a completed route.",
    "where": "game end screen, shared replay section, README",
    "test": "npm test -- --grep @claim:server-verified-replay",
    "sandbox": "fresh browser context at /demo; finish the board, inspect the opaque code, then submit an incomplete move log to the replay API"
  },
  {
    "id": "daily-and-archive",
    "claim": "Play one daily board and 20 permanent practice boards.",
    "where": "archive section, README",
    "test": "npm test -- --grep @claim:daily-and-archive",
    "sandbox": "fresh browser context at /demo; inspect archive and switch boards"
  },
  {
    "id": "daily-board-changes",
    "claim": "The daily board changes each UTC day.",
    "where": "archive section",
    "test": "npm test -- --grep @claim:daily-board-changes",
    "sandbox": "fresh browser context at / with two fixed UTC dates; compare visible daily board seeds"
  },
  {
    "id": "rally-card-values",
    "claim": "Fewer moves improve elegance and rescues increase the rescue value on a rally card.",
    "where": "game goal and rally card",
    "test": "npm test -- --grep @claim:rally-card-values",
    "sandbox": "fresh browser context at /demo; compare a direct completed route with a completed rescue detour"
  },
  {
    "id": "replay-code-private",
    "claim": "A replay code contains no name or profile.",
    "where": "shared replay section",
    "test": "npm test -- --grep @claim:replay-code-private",
    "sandbox": "finish /demo board, inspect the displayed code, then load it in an independent browser context"
  },
  {
    "id": "server-storage-metadata",
    "claim": "Replay records store a board, moves, opaque code, tenant, and creation time; demo records have a 24-hour expiry.",
    "where": "privacy page, README, demo documentation",
    "test": "cargo test --test server_api claim_server_storage_metadata",
    "sandbox": "fresh local SQLite; create public and demo records through the replay API, then inspect their persisted fields and demo expiry"
  },
  {
    "id": "completed-replay-only",
    "claim": "Only a completed route can become a replay code.",
    "where": "replay code error message",
    "test": "npm test -- --grep @claim:completed-replay-only",
    "sandbox": "fresh browser context at /demo; submit a correctly formatted but unfinished route"
  },
  {
    "id": "route-sound-after-move",
    "claim": "Route sounds begin only after a valid move.",
    "where": "settings dialog",
    "test": "npm test -- --grep @claim:route-sound-after-move",
    "sandbox": "fresh browser context at /demo with a Web Audio observer; start then make the first valid move"
  },
  {
    "id": "reduce-replay-motion",
    "claim": "Reduce movement shows shared replays without route animation.",
    "where": "settings dialog",
    "test": "npm test -- --grep @claim:reduce-replay-motion",
    "sandbox": "fresh browser context at /demo; enable Reduce movement and play the preloaded shared route"
  },
  {
    "id": "no-tracking",
    "claim": "The sample game makes no third-party requests.",
    "where": "privacy section, README",
    "test": "npm test -- --grep @claim:no-tracking",
    "sandbox": "fresh browser context at /demo; record all requests while playing and changing settings"
  },
  {
    "id": "fixed-60hz",
    "claim": "Use a measured 60 Hz update loop while a run is active.",
    "where": "game runtime note, README",
    "test": "npm test -- --grep @claim:fixed-60hz",
    "sandbox": "fresh browser context at /demo; sample fixed simulation ticks after starting"
  }
]
