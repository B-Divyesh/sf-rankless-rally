{
  "name": "rankless-rally",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && PORT=5173 npm run preview",
    "build": "tsc --noEmit && vite build",
    "preview": "cargo run --quiet --bin rankless-rally-server",
    "test": "npm run build && cargo test && playwright test",
    "test:claims": "npm run build && cargo test && playwright test tests/claims.spec.ts",
    "check": "npm run build && cargo test && npm run test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@types/node": "^22.13.4",
    "@playwright/test": "1.58.2",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
